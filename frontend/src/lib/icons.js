/**
 * Bộ icon dùng chung (Phosphor Icons).
 * Đặt tên theo *ý nghĩa* chứ không theo hình dáng, để đổi icon chỉ sửa ở file này.
 * Kích thước và độ dày mặc định khai báo bằng IconContext trong `App.jsx`.
 */
export {
  IconContext,

  // Điều khiển checkpoint
  Play as IconLaunch,
  Stop as IconStop,
  X as IconClose,
  Plus as IconAdd,
  Minus as IconRemove,
  Broadcast as IconLive,
  Timer as IconCountdown,
  ArrowUUpLeft as IconFollowUp,

  // Trạng thái
  Check as IconCheck,
  CheckCircle as IconCheckCircle,
  Warning as IconStuck,
  Question as IconUnclear,
  Hourglass as IconPending,
  Info as IconEvidence,
  ThumbsUp as IconThumbsUp,
  ThumbsDown as IconThumbsDown,

  // Điều hướng tài liệu
  CaretLeft as IconPrev,
  CaretRight as IconNext,
  CaretUp as IconCollapse,
  CaretDown as IconExpand,
  ArrowSquareOut as IconGoToSlide,
  CornersOut as IconPresent,
  MagnifyingGlassPlus as IconZoomIn,
  MagnifyingGlassMinus as IconZoomOut,

  // Học liệu
  FileText as IconMaterial,
  FilePdf as IconDoc,
  FolderOpen as IconChapter,
  Books as IconLibrary,
  NotePencil as IconNote,
  Highlighter as IconHighlight,

  // Vai trò & thao tác chung
  GearSix as IconSettings,
  SignOut as IconLogout,
  ArrowsClockwise as IconRefresh,
  Trash as IconDelete,

  // Trợ lý soạn câu hỏi
  MagicWand as IconAI,
  CircleNotch as IconSpinner,

  // Trạng thái rỗng
  Clock as IconWaiting,
  Target as IconNoCheckpoint,
  ClipboardText as IconNoReport,
  ChartBar as IconNoStats,
  Tray as IconEmpty,

  // Hình khối cho các phương án trả lời
  Triangle as IconShapeTriangle,
  Diamond as IconShapeDiamond,
  Circle as IconShapeCircle,
  Square as IconShapeSquare,
  Star as IconShapeStar,
  Sparkle as IconShapeSparkle,
} from '@phosphor-icons/react'
